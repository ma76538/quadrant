    import XCTest
    @testable import QuadrantKit

    final class QuadrantKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(QuadrantKit().text, "Hello, World!")
        }
    }
